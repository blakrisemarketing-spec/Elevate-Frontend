# Dependencies
node_modules/

# Build output
dist/
.tmp/

# Perf bench artifacts (screenshots + JSON) — dev-only, regenerated each run
outputs/perf/

# macOS
.DS_Store

# Context / local dev
.context/
tsconfig.tsbuildinfo

# Logs
*.log

# Environment
.env
.env.local
.env.*.local

# Server-side secrets (Hostinger PHP) — never commit
api/config.local.php
